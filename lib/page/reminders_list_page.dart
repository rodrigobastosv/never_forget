import 'package:flutter/material.dart';

class RemindersListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('REMINDERS LIST PAGE'),
      ),
    );
  }
}
